<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
    <head>
        <!-- shortcut:[Meta] -->
        <!-- title, description, keyword, robots -->
        {% if isSingle%}
            {% list Posts %}
            <title>{% if Post.Title is not empty %}{{ Post.Title }} - {% elseif Post.Snippet is not empty %}{{ Post.Snippet|truncate(80) }} - {% endif %}{{Blog.Title}}</title>
            <meta name="description" content="{{ Post.Snippet }}" />
            <meta name="keywords" content="{% list Post.Tags %}{{ Tag.Title }}{% if loop.last == false %}, {% endif %}{% endlist %}" />
                <meta name="robots" content="index,follow" />
            {% endlist %}
        {% elseif isTag and Tag is not empty %}
            <title>{{ Tag.Title|title }} - {{Blog.Title}}</title>
            <meta name="description" content="{{ Blog.Description }}" />
            <meta name="keywords" content="{{ Tag.Title|title }}" />
            <meta name="robots" content="index,follow" />
        {% elseif isSearch and Search is not empty %}
            <title>{{ Search|title }} - {{Blog.Title}}</title>
            <meta name="description" content="{{ Blog.Description }}" />
            <meta name="keywords" content="{{ Search|title }}" />
            <meta name="robots" content="noindex" />
        {% else %}
            <title>{{Blog.Title}}</title>
            <meta name="description" content="{{ Blog.Description }}" />
            <meta name="robots" content="index,follow" />
        {% endif %}
        <meta http-equiv="content-type" content="text/html; charset=utf-8" />
        
        <!-- Social setting : Facebook, G+ .. -->
        <meta property="og:site_name" content="{{ Blog.Title }}" />
        <meta property="og:type" content="blog" />
        <meta property="fb:app_id" content="{% if Custom('facebook_appid') is not empty %}{{ Custom('facebook_appid') }}{% endif %}" />
        <meta itemprop="name" content="{{ Blog.Title }}" />
        
        {% if isSingle %}
            {% list Posts %}
                <meta property="og:title" content="{% if Post.Title is not empty %}{{ Post.Title }} - {% elseif Post.Snippet is not empty %}{{ Post.Snippet|truncate(80) }}{% endif %}"/>
                <meta property="og:description" content="{{ Post.Snippet }}" />
                <meta itemprop="description" content="{{ Post.Snippet }}" />
                <meta property="og:url" content="{{ Blog.Url }}{{ Post.Permalink }}" />
                {% if Post.Cover %}
                    <meta property="og:image" content="{{ Post.Cover }}"/>
                    <meta itemprop="image" content="{{ Post.Cover }}" />
                {% endif %}
            {% endlist %}
            <meta property="og:type" content="overblogkiwi:see" />
        {% else %}
            <meta property="og:title" content="{{ Blog.Title }}"/>
            <meta property="og:description" content="{{ Blog.Description }}" />
            <meta itemprop="description" content="{{ Blog.Description }}" />
            {% if Blog.Avatar %}
                <meta property="og:image" content="{{ Blog.Avatar }}"/>
                <meta itemprop="image" content="{{ Blog.Avatar }}" />
            {% else %}
                {% list Posts %}
                    {% if loop.index == 1 and Post.Cover %}
                        <meta property="og:image" content="{{ Post.Cover }}"/>
                        <meta itemprop="image" content="{{ Post.Cover }}" />
                    {% endif %}
                {% endlist %}
            {% endif %}
            {% if isSearch %}
                <meta property="og:type" content="overblogkiwi:search" />
            {% else %}
                <meta property="og:type" content="overblogkiwi:visit" />
            {% endif %}
        {% endif %}

        <!-- shortcut:[Includes] -->
        <!-- favicon -->
        {% if Blog.FaviconUrl is not empty %}
            <link rel="icon" type="image/png" href="{{ Blog.FaviconUrl }}" />
        {% else %}
            <link rel="shortcut icon" type="image/x-icon" href="http://fdata.over-blog.net/99/00/00/01/img/favicon.ico" />
            <link rel="icon" type="image/png" href="http://fdata.over-blog.net/99/00/00/01/img/favicon.png" />
            <link rel="apple-touch-icon" href="http://fdata.over-blog.net/99/00/00/01/img/mobile-favicon.png" />
        {% endif %}	
        
        <!-- SEO -->
        {% if Custom('google_webmaster_id') is not empty %}
            <meta name="google-site-verification" content="{{ Custom('google_webmaster_id') }}" />
        {% endif %}
        {% if Custom('msn_webmaster_id') is not empty %}
            <meta name="msvalidate.01" content="{{ Custom('msn_webmaster_id') }}" />
        {% endif %}

        <!-- includes -->
        <link rel="alternate" type="application/rss+xml" title="RSS" href="{{ Blog.RssUrl }}" />
        {% if Blog.CssUrl is not empty %}
            <link rel="stylesheet" href="{{ Blog.CssUrl }}" type="text/css" />
        {% endif %}

        <!-- Style -->
        <style type="text/css">
            /*----- Reset -----*/
            body,div,dl,dt,dd,ul,ol,li,h1,h2,h3,h4,h5,h6,pre,form,fieldset,input,p,textarea,blockquote,th,td{ margin:0; padding:0;}
            table{ border-collapse:collapse; border-spacing:0;}
            fieldset,img{ border:0;}
            address,caption,cite,code,dfn,var{ font-style:normal; font-weight:normal;}
            ol,ul{ list-style:none;}
            caption,th{ text-align:left;}
            h1,h2,h3,h4,h5,h6{ font-size:100%; font-weight:normal;}
            q:before,q:after{ content:'';}
            abbr,acronym{ border:0;}
        </style>
        
        <!-- jQuery -->
        <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js"></script>
        
        <!-- NivoSlider -->
        <link rel="stylesheet" href="http://assets.over-blog-kiwi.com/themes/jquery/nivo-slider/nivo-slider.css" type="text/css" media="screen" />
        <link rel="stylesheet" href="http://assets.over-blog-kiwi.com/themes/jquery/nivo-slider/themes/default/default.css" type="text/css" />
        <script src="http://assets.over-blog-kiwi.com/themes/jquery/nivo-slider/jquery.nivo.slider.pack.js" type="text/javascript"></script>

        <!-- Fancybox -->
        <script src="http://assets.over-blog-kiwi.com/themes/jquery/fancybox/jquery.fancybox-1.3.4.pack.js" type="text/javascript"></script>
        <link rel="stylesheet" type="text/css" href="http://assets.over-blog-kiwi.com/themes/jquery/fancybox/jquery.fancybox-1.3.4.css" media="screen" />
        
        <!-- Template resources -->
        <link href='http://fonts.googleapis.com/css?family=Marvel|Ubuntu+Condensed|Delius+Unicase|Arvo' rel='stylesheet' type='text/css'>
        <style type="text/css">
            /*  
            Theme Name: Captive Green
            Theme URI: http://www.freewpthemes.net/preview/captivegreen
            Description: Widget ready and tested on WP 2.3.1.
            Version: 2.0
            Author: Free WordPress Themes
            Author URI: http://www.freewpthemes.net/
            */

            body {
                margin: 0;
                padding: 0;
                background: #EEEBDA url(http://www.efusia.net/overblog/captivegreen/images/img01.jpg) repeat-x left top;
                font-family: Arial, Helvetica, sans-serif;
                font-size: 14px;
                color: #4D4D4D;
            }

            h1, h2, h3 {
                margin: 0;
                padding: 0;
                font-weight: normal;
                font-family: 'Arvo', serif;
                color: #056303;
            }

            h1 {
                font-size: 2em;
            }

            h2 {
                font-size: 2.8em;
            }

            h3 {
                font-size: 1.6em;
            }

            p, ul, ol {
                margin-top: 0;
                line-height: 150%;
            }

            ul, ol {
            }

            a {
                text-decoration: none;
                color: #056303;
            }

            a:hover {
            }

            #wrapper {
                background: url(http://www.efusia.net/overblog/captivegreen/images/img03.jpg) repeat-y center top;
            }

            .container {
                width: 1000px;
                margin: 0px auto;
            }

            /* Header */

            #header {
                width: 940px;
                height: 100px;
                margin: 0px auto;
                padding: 0px;
            }

            /* Logo */

            #logo {
                float: left;
                width: 288px;
                height: 100px;
                margin: 0;
                padding: 0;
                color: #FFFFFF;
            }

            #logo h1, #logo p {
            }

            #logo h1 {
                padding: 30px 0px 0px 0px;
                letter-spacing: -3px;
                text-align: center;
                text-transform: lowercase;
                font-size: 40px;
            }

            #logo h1 span {
                color: #8AD500;
            }

            #logo h1 a {
                color: #FFFFFF;
            }

            #logo p {
                margin: 0;
                padding: 0px 0 0 0px;
                letter-spacing: 1px;
                text-transform: uppercase;
                font-size: 12px;
                color: #FFFFFF;
            }

            #logo p a {
                color: #FFFFFF;
            }

            #logo a {
                border: none;
                background: none;
                text-decoration: none;
                color: #000000;
            }

            /* Search */

            #search {
            }

            #search form {
                height: 41px;
                margin: 0;
                padding: 10px 0 0 20px;
            }

            #search fieldset {
                margin: 0;
                padding: 0;
                border: none;
            }

            #search-text {
                width: 170px;
                padding: 6px 5px 3px 5px;
                border: none;
                background: #D0C9B7;
                text-transform: lowercase;
                font: normal 11px Arial, Helvetica, sans-serif;
            }

            #search-submit {
                width: 50px;
                height: 22px;
                border: none;
                background: #1D1D1D;
                color: #FFF;
            }

            /* Menu */

            #menu {
                float: right;
                width: 652px;
                height: 100px;
                margin: 0px auto;
                padding: 0px;
            }

            #menu ul {
                float: right;
                margin: 0;
                padding: 50px 20px 0px 0px;
                list-style: none;
                line-height: normal;
            }

            #menu li {
                float: left;
                padding: 0px 15px 0px 15px;
            }

            #menu a {
                display: block;
                height: 20px;
                margin-right: 1px;
                letter-spacing: 1px;
                text-decoration: none;
                text-align: center;
                text-transform: uppercase;
                font-family: 'Arvo', serif;
                font-size: 14px;
                font-weight: normal;
                color: #FFFFFF;
                border: none;
            }

            #menu a:hover, #menu .current_page_item a {
                text-decoration: none;
            }

            #menu .current_page_item a {
            }

            /* Page */

            #page {
                width: 940px;
                margin: 0px auto;
                padding: 30px 0px;
            }

            /* Content */

            #content {
                float: right;
                width: 600px;
                padding: 0px 26px;
            }

            .post {
                overflow: hidden;
                margin-bottom: 20px;
                border-bottom: 1px dashed #CACACA;
            }

            .post .title {
                margin: 0px;
                padding: 7px 0px 0px 0px;
                letter-spacing: -1px;
            }

            .post .title a {
                border: none;
                letter-spacing: 1px;
                letter-spacing: -1px;
                text-align: left;
                text-transform: uppercase;
                font-size: 26px;
                font-weight: normal;
                color: #056303;
            }

            .post .meta {
                margin-bottom: 30px;
                padding: 0px;
                letter-spacing: 1px;
                text-align: left;
                text-transform: uppercase;
                font-family: 'Arvo', serif;
                font-size: 12px;
                font-weight: normal;
            }

            .post .meta .date {
                float: left;
            }

            .post .meta .posted {
                float: right;
            }

            .post .meta a {
            }

            .post .entry {
                padding: 0px 0px 30px 0px;
                padding-bottom: 20px;
                text-align: justify;
            }

            .links {
                float: right;
                margin: 20px 0px 30px 0px;
                text-align: right;
                text-transform: uppercase;
                font-family: 'Arvo', serif;
                font-size: 12px;
                font-weight: normal;
            }

            .more {
                display: block;
                float: left;
                padding: 0px 0px 0px 0px;
                margin-right: 30px;
            }

            .comments {
                display: block;
                float: left;
                padding: 0px 0px 0px 0px;
            }

            /* Sidebar */

            #sidebar {
                float: left;
                width: 288px;
                margin: 0px;
                color: #726B63;
            }

            #sidebar ul {
                margin: 0;
                padding: 0;
                list-style: none;
            }

            #sidebar li {
                margin: 0;
                padding: 0;
            }

            #sidebar li ul {
                margin: 0px 0px;
                padding-bottom: 50px;
            }

            #sidebar li li {
                line-height: 35px;
                border-bottom: 1px dashed #A5A08C;
                margin: 0px 30px;
                font-size: 14px;
                font-weight: normal;
            }

            #sidebar li li span {
            }

            #sidebar li li a {
                padding: 0px 0px 0px 0px;
            }

            #sidebar h2 {
                margin: 0px;
                padding: 0px 0px 10px 0px;
                padding-left: 30px;
                text-align: left;
                text-transform: uppercase;
                font-size: 20px;
                font-weight: normal;
                color: #2E2E2E;
            }

            #sidebar p {
                margin: 0 0px;
                padding: 0px 30px 20px 30px;
                text-align: justify;
                font-size: 14px;
            }

            #sidebar a {
                border: none;
                color: #857E6E;
            }

            #sidebar a:hover {
                text-decoration: underline;
            }

            /* Calendar */

            #calendar {
            }

            #calendar_wrap {
                padding: 20px;
            }

            #calendar table {
                width: 100%;
            }

            #calendar tbody td {
                text-align: center;
            }

            #calendar #next {
                text-align: right;
            }

            /* Footer */

            #footer {
                width: 940px;
                margin: 0px auto;
                padding: 20px 0px;
                background: #3D3630;
                color: #949085;
            }

            #footer p {
                margin: 0;
                line-height: normal;
                letter-spacing: 2px;
                text-align: center;
                text-decoration: none;
                text-align: center;
                text-transform: uppercase;
                font-family: 'Arvo', serif;
                font-weight: normal;
                font-size: 10px;
            }

            #footer a {
                color: #949085;
            }

            #banner {
                width: 940px;
                margin: 0px auto;
                height: 150px;
            }

            .wp-caption {
            }

            .wp-caption-text {
            }

            .sticky {
            }

            .gallery-caption {
            }

            .bypostauthor {
            }

            .alignright {
            }

            .alignleft {
            }

            .aligncenter {
            }

            .tags {
                font: inherit;
            }

            #tags {
                font: inherit;
            }

            #tags {}
        </style>
        <style type="text/css">
            .clear{
                clear: both;
            }
            .ob-text ol,
            .ob-text ul{
                margin: 0 0 0 35px;
            }
            .ob-text ol li,
            .ob-text ul li{
                list-style: disc;
                margin: 3px 0;
            }
            .ob-text ol li{
                list-style: decimal;
            }
            #header{
                height: 250px;
            }
            #logo{
                {% if Custom("custom_menu_background_color") is not empty %}
                    background: {{ Custom("custom_menu_background_color") }};
                {% else %}
                    background: #131313;
                    background-color: rgba(19, 19, 19, 0.85);
                {% endif %}
                height: auto;
                margin: 20px 0 0 0;
            }
            #logo h1{
                padding: 15px 0 0 0;
            }
            .ob-section-video,
            .ob-section-video iframe {
                width: 600px;
            }
            #page{
                background: url("http://www.freewpthemes.net/themes/previews/captivegreen/images/img03.jpg") repeat-y -30px 0;
            }
            #sidebar h2 a{
                color: #2E2E2E;
            }
            #wrapper{
                background: none;
            }
            {% if Custom("custom_page_background_color") is not empty %}
                body{
                    background: {{ Custom("custom_page_background_color") }};
                }
            {% endif %}
            {% if Custom("custom_footer_background_color") is not empty %}
                #footer{
                    background: {{ Custom("custom_footer_background_color") }};
                }
            {% endif %}
            {% if Custom("custom_header_background") is not empty %}
                #wrapper2{
                    background: url("{{ Custom("custom_header_background") | resize(1000,350) | crop }}") no-repeat scroll center top;
                }
                #menu ul{
                    {% if Custom("custom_menu_background_color") is not empty %}
                        background: {{ Custom("custom_menu_background_color") }};
                    {% else %}
                        background: #131313;
                        background-color: rgba(19, 19, 19, 0.7);
                    {% endif %}
                    margin: 35px 0 0 0;
                    padding: 15px 20px;
                }
                {% if Custom("custom_menu_background_color") is not empty %}
                    #menu ul a{
                        border-bottom: solid 2px {{ Custom("custom_menu_background_color") }};
                    }
                {% endif %}
                #menu ul li.current_page_item a,
                #menu ul a:hover{
                    border-bottom: solid 2px #FFF;
                    text-decoration: none;
                }
            {% endif %}
            {% if Custom("custom_menu_font_color") is not empty %}
                #menu a,
                #logo h1 a{
                    color: {{ Custom("custom_menu_font_color") }};
                }
                #menu ul li.current_page_item a,
                #menu ul a:hover{
                    border-bottom: solid 2px {{ Custom("custom_menu_font_color") }};
                }
            {% endif %}
            {% if Custom("custom_footer_font_color") is not empty %}
                #footer{
                    color: {{ Custom("custom_footer_font_color") }};
                }
            {% endif %}
            
            #sidebar .m-networks a,
            #sidebar .m-networks a:hover,
            #sidebar .m-networks img,
            #sidebar .m-networks img:hover {
                margin-right: 4px;
                text-decoration: none;
            }
            #sidebar .m-networks a:last-child{
                margin-right: 0;
            }
            .obsoundplayername{
                max-width: 225px;
            }
            .entry{
                word-wrap: break-word;
            }
            .ob-text h2{
                font-size: 1.8em;
            }
            .ob-text h2,
            .ob-text h3,
            .ob-text h4{
                color: #056303;
                font-family: 'Arvo', serif;
                font-weight: 400;
                letter-spacing: -1px;
                margin: 14px 0 7px 0;
            }
            .ob-text pre{
                border: 1px dotted #CCC;
                margin: 7px;
                padding: 7px;
            }
            .ob-section p,
            .ob-section ul,
            .ob-section ol{
                line-height: 1.5em;
            }
            .ob-section p {margin-bottom: 15px;}
            
            .ob-section-file,
            .ob-ctn {
	            margin-left: 0;
            }
            .ob-section .ob-snippet {
	            line-height: 1em;
            }
            .ob-input-submit{
                background: #EEE;
                border: solid 1px #BBB;
                -moz-border-radius: 3px;
                -webkit-border-radius: 3px;
                -ms-border-radius: 3px;
                -o-border-radius: 3px;
                border-radius: 3px;
                cursor: pointer;
                padding: 6px 20px;
            }
            .ob-input-submit:hover{
                background: #DDD;
            }
            .ob-form-field{
                margin-bottom: 10px;
            }
            #navigation .alignright {
                float: right;
            }
            #navigation .alignleft {
                float: left;
            }
            .meta a{
                color: #4D4D4D;
            }
            .m-social-networks h3 {
            	padding-bottom: 10px;
            }
            .fb-like iframe {
	            margin-top: -3px;
            }
            a.comments{
                background: url(http://www.efusia.net/overblog/captivegreen/images/comments.png) no-repeat top left;
                margin: 0 0 0 22px;
                padding: 0 0 0 22px;
            }
        </style>
        
        <!-- Custom vars -->
        <meta name="constant:displayArticlesLimit" content="10" />
        
        <meta name="image:custom_header_background:Header background image" default="http://www.efusia.net/overblog/captivegreen/images/img02.jpg" group="Design" />
        <meta name="color:custom_page_background_color:Page background color" default="#EEEBDA" group="Design" />
        <meta name="color:custom_footer_background_color:Footer background color" default="#3D3630" group="Design" />
        <meta name="color:custom_footer_font_color:Footer font color" default="#949085" group="Design" />
        <meta name="color:custom_menu_background_color:Menu background color" default="#131313" group="Design" />
        <meta name="color:custom_menu_font_color:Menu font color" default="#FFFFFF" group="Design" />
        
        <meta name="boolean:custom_hide_widget_description:Hide Widget &quot;Blog Description&quot;" group="Widgets" />
        <meta name="boolean:custom_hide_widget_search_form:Hide Widget &quot;Search form&quot;" group="Widgets" />
        <meta name="boolean:custom_hide_widget_biography:Hide Widget &quot;Biography&quot;" group="Widgets" />
        <meta name="boolean:custom_hide_widget_categories:Hide Widget &quot;Categories&quot;" group="Widgets" />
        <meta name="boolean:custom_hide_widget_pages:Hide Widget &quot;Pages&quot;" group="Widgets" />
        <meta name="boolean:custom_hide_widget_archives:Hide Widget &quot;Archives&quot;" group="Widgets" />
        
        <meta name="string:facebook_account:Enter your FaceBook account" default="overblog" group="Social" />
        <meta name="string:twitter_account:Enter your Twitter account" default="overblog" group="Social" />

        <meta name="list:comment:Comment service" items="OverBlog, Facebook, Disqus" default="OverBlog" group="Comments" />
        <meta name="string:comment_disqus:Disqus website's shortname (Mandatory)" group="Comments" />
        <meta name="list:custom_facebook_comments_color_theme:Facebook comments color theme" items="light, dark" default="light" group="Comments" />
        
        <meta name="string:custom_footer:Enter your footer text" default="Copyright" group="Settings" />
        <meta name="string:google_analytics:Enter your Google Analytics ID" group="Settings" />
        <meta name="string:facebook_appid:Enter your FaceBook App ID" group="Settings" />
        <meta name="string:google_webmaster_id:Enter your Google Webmaster Tools ID" group="Settings" />
        <meta name="string:msn_webmaster_id:Enter your MSN Webmaster Tools ID" group="Settings" />
    </head>
    <body>
        <div id="wrapper">
            <div id="wrapper2">
                <div id="header-wrapper">
                    <div id="header">
                        <div id="logo">
                            <h1>
                                <a href="{{ Blog.Url }}" title="{{ Blog.Description }}">{{ Blog.Title|truncate(50, true) }}</a>
                            </h1>
                        </div>
                        <div id="menu">
                            <ul>
                                {% list Blog.PostsTags(4, 'SORT_POSTS_NUMBER_DESC') %}
                                    <li {% if isTag and Tag is not empty and Tag.Title == PostsTag.Title %} class="current_page_item" {% endif %}><a href="{{ PostsTag.Url }}">{{ PostsTag.Title }}</a></li>
                                {% endlist %}
                                <li><a href="/contact">{{ Lang.Get('Contact') }}</a></li>
                            </ul>
                        </div>
                        <div style="clear: both;">&nbsp;</div>
                    </div>
                </div>
                <div id="page">
                
                    <!-- shortcut:[Main content] -->
                    <div id="content">
                        {% if isSpecial %}
                            {{SpecialContent}}
                        {% elseif isSingle %}
                            <!-- Single Page -->
                            <div class="post">
                                <h2 class="title">
                                    <a title="{{ Lang.Get('More about') }} {% if Post.Title is not empty %}&quot;{{ Post.Title }}&quot;{% else %}{{ Lang.Get('this post') }}{% endif %}" href="{{ Post.PermaLink }}">
                                        {% if Post.Title is not empty %}
                                            {{ Post.Title }}
                                        {% else %}
                                            {{ Lang.Get('From') }} 
                                            {% list Post.Sources %}
                                                {{ Lang.Get(Source) }}
                                                {% if not loop.last %}
                                                     {{ Lang.Get(' and ') }} 
                                                {% endif %}
                                            {% endlist %}
                                        {% endif %}
                                    </a>
                                </h2>
                                <p class="meta">
                                    <span class="date">{{ Post.Date|date("M d Y, H:ia") }}</span>
                                    {% if Post.Author and Post.Author is not empty %}
                                        <span class="posted">{{ Lang.Get('Posted by') }} {{Post.Author}}</span>
                                    {% endif %}
                                </p>
                                {% list Post.Tags %}
                                    {% if loop.index == 1 %}
                                        <p class="meta">
                                            <strong>{{ Lang.Get('Tags:') }}</strong>
                                    {% endif %}
                                    <a href="{{Tag.Url}}" title="{{Tag.Title}}">{{Tag.Title}}</a>
                                    {% if not loop.last %}
                                        , 
                                    {% endif %}
                                    {% if loop.length > 0 and loop.last %}
                                         </p>
                                    {% endif %}
                                {% endlist %}
                                <div class="entry">
                                    <p>{{ Post.Body }}</p>
                                </div>
                                <div class="ob-section m-social-networks">
                                	<h3>{{ Lang.Get('Share this post') }}</h3>
                                    <!-- Social networks : share -->
                                    <div class="fb-like" data-href="{{ Blog.Url }}{{ Post.PermaLink }}" data-send="true" data-layout="button_count" data-width="120" data-show-faces="true" data-font="lucida grande"></div>
                                    <a href="https://twitter.com/share" class="twitter-share-button" data-text="{% if Post.Title is not empty %}{{ Post.Title }}{% elseif Post.Snippet is not empty %}{{ Post.Snippet}}{% else %}{{ Blog.Title }}{% endif %}" data-url="{{ Blog.Url }}{{ Post.PermaLink }}" data-lang="{{ Blog.Lang }}" {% if Custom('twitter_account') is not empty %} data-via="{{Custom('twitter_account')}}" {% endif %}></a>
                                    <g:plusone href="{{ Blog.Url }}{{ Post.PermaLink }}" size="medium"></g:plusone>
                                </div>
                                {% if not isPage %}
                                    <div class="bloc-comment" id="comments">
                                        <!-- Comments -->
                                        <div class="item-comments">
                                            {% if Custom("comment") == "Facebook" %}
                                                <div class="fb-comments" data-href="{{ Blog.Url }}{{ Post.Permalink }}" data-num-posts="2" data-width="600" data-colorscheme="{{ Custom("custom_facebook_comments_color_theme")|default('light') }}"></div>
                                            {% elseif Custom("comment") == "Disqus" and Custom('comment_disqus') is not empty %}
                                                <div id="disqus_thread"></div>
                                                <script type="text/javascript">
                                                    var disqus_shortname = '{{ Custom('comment_disqus') }}';
                                                    (function() {
                                                        var dsq = document.createElement('script'); dsq.type = 'text/javascript'; dsq.async = true;
                                                        dsq.src = 'http://' + disqus_shortname + '.disqus.com/embed.js';
                                                        (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
                                                    })();
                                                </script>
                                                <noscript>Please enable JavaScript to view the <a href="http://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>
                                            {% else %}
                                                {{ Post.Comments }}
                                            {% endif %}
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
                        {% else %}
                            <!-- Other pages -->
                            {% list Posts %}
                                <div class="post">
                                    <h2 class="title">
                                        <a title="{{ Lang.Get('More about') }} {% if Post.Title is not empty %}&quot;{{ Post.Title }}&quot;{% else %}{{ Lang.Get('this post') }}{% endif %}" href="{{ Post.PermaLink }}">
                                            {% if Post.Title is not empty %}
                                                {{ Post.Title }}
                                            {% else %}
                                                {{ Lang.Get('From') }} 
                                                {% list Post.Sources %}
                                                    {{ Lang.Get(Source) }}
                                                    {% if not loop.last %}
                                                         {{ Lang.Get('and') }} 
                                                    {% endif %}
                                                {% endlist %}
                                            {% endif %}
                                        </a>
                                    </h2>
                                    <p class="meta">
                                        <span class="date">{{ Post.Date|date("M d Y, H:ia") }}</span>
                                        {% if Post.Author and Post.Author is not empty %}
                                            <span class="posted">{{ Lang.Get('Posted by') }} {{Post.Author}}</span>
                                        {% endif %}
                                        
                                        <!-- Comments count -->
                                        {% if Custom("comment") == "Disqus" and Custom('comment_disqus') is not empty %}
                                            <a title="{{ Lang.Get('comments') }}" class="comments" href="{{ Blog.Url }}{{ Post.PermaLink }}#disqus_thread"></a>
                                        {% else %}
                                            <a title="{{ Lang.Get('comments') }}" class="comments" href="{{ Post.PermaLink }}#comments">
                                                {% if Custom("comment") == "Facebook" %}
                                                    <fb:comments-count href={{ Blog.Url }}{{ Post.Permalink }}></fb:comments-count> {{ Lang.Get('comments') }}
                                                {% else %}
                                                    {{ Post.CommentCount(Lang.Get('comment'), Lang.Get('comments')) }}
                                                {% endif %}
                                            </a>
                                        {% endif %}
                                    </p>
                                    <div class="entry clear">
                                        {% if Post.Cover is not empty %}
                                            {% if isIndex %}
                                                <img class="ob-media ob-img ob-pull-left ob-media-left" src="{{ Post.Cover|resize(300, 225) }}" alt="" />
                                            {% else %}
                                                <img class="ob-media ob-img ob-pull-left ob-media-left" src="{{ Post.Cover|resize(200, 150) }}" alt="" />
                                            {% endif %}
                                        {% endif %}
                                        <p>{{ Post.Snippet }}</p>
                                    </div>
                                </div>
                            {% endlist %}
                            
                            {% if isSearch and Posts is empty %}
                                <!-- No result for search -->
                                <p>{{ Lang.Get('No result found') }}</p>
                            {% endif %}
                        {% endif %}
                        
                        <!-- Pagination -->
                        {% if Navigation.PreviousPage is not empty or Navigation.NextPage is not empty or Navigation.PreviousPost is not empty or Navigation.NextPost is not empty and isPage == false %}
                            <div id="navigation">
                                {% if Navigation.PreviousPage is not empty %}
                                    <div class="alignleft">
                                    	<a href="{{ Navigation.PreviousPage }}" class="prev">
                                    		{{ Lang.Get('Previous Entries') }}
                                    	</a>
                                    </div>
                                {% elseif Navigation.PreviousPost is not empty %}
				                	 <div class="alignleft">
                                    	<a href="{{ Navigation.PreviousPost.Permalink }}" class="prev" title="{{ Navigation.PreviousPost.Title }}">
                                    		{{ Lang.Get('Previous Post') }}
                                    	</a>
                                    </div>
				                {% endif %}
                                {% if Navigation.NextPage is not empty %}
                                    <div class="alignright">
                                    	<a href="{{ Navigation.NextPage }}" class="next">
                                    	{{ Lang.Get('Next Entries') }}</a></div>
                                {% elseif Navigation.NextPost is not empty %}
                                    <div class="alignright">
                                    	<a href="{{ Navigation.NextPost.Permalink }}" class="next" title="{{ Navigation.NextPost.Title }}">
                                    	{{ Lang.Get('Next Post') }}</a>
                                    </div>
                                {% endif %}
                            </div>
                        {% endif %}
                    </div>
                    <!-- shortcut:[Sidebar content] -->
                    <div id="sidebar">
                        <ul>
                            <!-- Search form -->
                            {% if Custom('custom_hide_widget_search_form') == false %}
                                <li id="search" class="widget widget_search">
                                    <h2>{{ Lang.Get('Search') }}</h2>
                                    <div id="search" >
                                        <form class="search" id="search" method="get" action="/search/">
                                            <div>
                                                <input type="text" name="q" placeholder="{{ Lang.Get('Search') }}..."  id="search-text" value="{{ Search }}" />
                                            </div>
                                        </form>
                                    </div>
                                </li>
                            {% endif %}
                            
                            <!-- Blog description -->
                            {% if Custom('custom_hide_widget_description') == false %}
                                {% if Blog.Description is not empty %}
                                    <li>
                                        <p>{{ Blog.Description }}</p>
                                    </li>
                                {% endif %}
                            {% endif %}
                            
                            <!-- Blog owner -->
                            {% if Custom('custom_hide_widget_biography') == false %}
                                {% if Blog.Owner.Nickname is not empty and Blog.Owner.Bio is not empty %}
                                    <li>
                                        <h2>
                                            {{ Blog.Owner.Nickname }}
                                        </h2>
                                        {% if Blog.Owner.Bio is not empty %}
                                            <p>
                                                {% if Blog.Owner.Avatar is not empty %}
                                                    <a title="{{ Blog.Owner.Nickname }}" class="fancybox ob-media ob-img ob-pull-left ob-media-left" href="{{ Blog.Owner.Avatar }}"><img src="{{ Blog.Owner.Avatar|resize(60,60) }}" alt="" /></a>
                                                {% endif %}
                                                {{Blog.Owner.Bio}}
                                            </p>
                                        {% endif %}
                                        <p class="m-networks">
                                            {% if Custom('facebook_account') is not empty or Custom('twitter_account') is not empty %}
                                                {% if Custom('facebook_account') is not empty %}
                                                    <a title="Facebook" target="_blank" href="http://www.facebook.com/{{ Custom('facebook_account') }}">
                                                    	<img alt="Facebook" src="http://www.efusia.net/overblog/captivegreen/images/facebook.png" />
                                                    </a>
                                                {% endif %}
                                                {% if Custom('twitter_account') is not empty %}
                                                    <a title="Twitter" target="_blank" href="https://www.twitter.com/{{ Custom('twitter_account') }}">
                                                    	<img alt="Twitter" src="http://www.efusia.net/overblog/captivegreen/images/twitter.png" />
                                                    </a>
                                                {% endif %}
                                            {% endif %}
                                            <a title="RSS" target="_blank" href="{{ Blog.RssUrl }}">
                                            	<img alt="Twitter" src="http://www.efusia.net/overblog/captivegreen/images/rss.png" />
                                            </a>
                                        </p>
                                    </li>
                                {% endif %}
                            {% endif %}
                            
                            <!-- Tags -->
                            {% if Custom('custom_hide_widget_categories') == false %}
                                {% if Blog.PostsTags() and Blog.PostsTags() is not empty %}
                                    <li>
                                        <h2>{{ Lang.Get('Tags') }}</h2>
                                        <ul>
                                            {% list Blog.PostsTags(10) %}
                                                <li><a href="{{ PostsTag.Url }}">{{ PostsTag.Title }} ({{ PostsTag.PostCount }})</a></li>
                                            {% endlist %}
                                        </ul>
                                    </li>
                                {% endif %}
                            {% endif %}
                            
                            <!-- Pages -->
                            {% if Custom('custom_hide_widget_pages') == false %}
                                {% if Blog.ListPages and Blog.ListPages is not empty %}
                                     <li>
                                        <h2>{{ Lang.Get('Pages') }}</h2>
                                        <ul>
                                            {% list Blog.ListPages %}
                                                <li>
                                                    <a href="{{ListPage.Url}}">{{ListPage.Title}}</a>
                                                </li>
                                            {% endlist %} 
                                        </ul>
                                    </li>
                                {% endif %}
                            {% endif %}
                            
                            <!-- Archives -->
                            {% if Custom('custom_hide_widget_archives') == false %}
                                {% if Blog.Archives is not empty %}
                                    <li>
                                        <h2>{{ Lang.Get('Archives') }}</h2>
                                        <ul id="archivelist">
                                            {% list Blog.Archives %}
                                                <li>
                                                    <a href="#" class"plustext" onclick="javascript: return switch_arch('{{Archive.Year}}')">{{Archive.Year}}</a>
                                                    <ul id="arch_{{Archive.Year}}" class="arch_month">
                                                    {% list Archive.Months %}
                                                        {% if Month.PostCount > 0 %}
                                                            <li><a href="{{Month.Url}}">{{Month.Name}}</a> ({{Month.PostCount}})</li>
                                                        {% endif %}
                                                    {% endlist %}
                                                    </ul>
                                                </li>
                                            {% endlist %}
                                        </ul>
                                    </li>
                                {% endif %}
                            {% endif %}
                        </ul>
                    </div>
                    <div style="clear: both; height: 20px;">&nbsp;</div>
                </div>
            </div>
        </div>
        <div id="footer-wrapper">
            <div id="footer">
                {% if Custom('custom_footer') is not empty %}
                    <p id="legal">{{ Custom('custom_footer') }}</p>
                {% endif %}
            </div>
        </div>
        
        <!-- Google Analytics -->
        {% if Custom('google_analytics') is not empty %}
            <script type="text/javascript">
                window.google_analytics_uacct = '{{ Custom('google_analytics') }}';
                var _gaq = _gaq || [];
                _gaq.push(['_setAccount', '{{ Custom('google_analytics') }}']);
                _gaq.push(['_trackPageview']);
                _gaq.push(['_trackPageLoadTime']);

                (function() {
                    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
                    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
                    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
                })();
            </script>
        {% endif %}
        
        <!-- Archives -->
        {% if Custom('custom_hide_widget_archives') == false %}
            <script type="text/javascript">
                function switch_arch(year) {
                    'use strict';
                    var archiveYear, archiveMonths, displayStyle;
                    displayStyle = '';
                    archiveYear = document.getElementById('arch_' + year);
                    if (typeof (archiveYear) !== 'undefined') {
                        if (archiveYear.style.display !== 'none') {
                            // Hide years without months
                            archiveMonths = archiveYear.getElementsByTagName('li');
                            if (!archiveMonths.length) {
                                archiveYear.parentNode.style.display = 'none';
                            }
                            // Onclick management
                            displayStyle = 'none';
                        }
                        archiveYear.style.display = displayStyle;
                    }
                    return false;
                }
            </script>
        {% endif%}
        
        <!-- Social networks : Facebook -->
        <div id="fb-root"></div>
        <script type="text/javascript">
            (function(d, s, id) {
                var js, fjs = d.getElementsByTagName(s)[0];
                if (d.getElementById(id)) return;
                js = d.createElement(s); js.id = id;
                js.src = "//connect.facebook.net/en_US/all.js#xfbml=1&appId={{ Custom('facebook_appid')|default('284865384904712')}}";
                fjs.parentNode.insertBefore(js, fjs);
            }(document, 'script', 'facebook-jssdk'));
        </script>
        
        <!-- Social networks : Twitter -->
        <script type="text/javascript">!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0];if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src="//platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs); }}(document,"script","twitter-wjs");</script>
        
        <!-- Social networks : Google + -->
		<script type="text/javascript">
		  window.___gcfg = {lang: '{{ Blog.Lang }}'};
		
		  (function() {
		    var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true;
		    po.src = 'https://apis.google.com/js/plusone.js';
		    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(po, s);
		  })();
		</script>
        
        <!-- jQuery onReady -->
        <script type="text/javascript">
            jQuery(document).ready(function() {
                // Fancy box
                jQuery('.ob-section-images a, .ob-link-img').attr('rel', 'fancyGroup');
                $("a[rel=fancyGroup]").fancybox({
                    'overlayShow'	: true,
                    'transitionIn'	: 'elastic',
                    'transitionOut'	: 'elastic',
                    'type': 'image'
                });
                
                (function() {
                    {% if Custom('custom_hide_widget_search_form') == false %}
                        // HTML5 placeholder fallback
                        function isPlaceholderSupported() {
                            'use strict';
                            var o = document.createElement('input');
                            return 'placeholder' in o;
                        }
                        function initPlaceHolder(el){
                            if(jQuery.trim(el.val()) === '' || jQuery.trim(el.val()) === el.attr('placeholder')){
                                el.val(el.attr('placeholder'));
                                el.css({'color': '#999'});
                            }
                        }
                        function togglePlaceholder(el, submitMode) {
                            'use strict';
                            var v = jQuery.trim(el.val()), p = el.attr('placeholder');
                            if (v === '' && !submitMode) {
                                el.val(p);
                                el.css({'color': '#999'});
                            } else if (v === p) {
                                el.val('');
                            }
                        }
                        function emulatePlaceholder(form) {
                            'use strict';
                            if (!isPlaceholderSupported()) {
                                jQuery('input[placeholder]', form).each(function () {
                                    initPlaceHolder(jQuery(this));
                                }).focus(function () {
                                    togglePlaceholder(jQuery(this), false);
                                }).blur(function () {
                                    initPlaceHolder(jQuery(this));
                                });
                                form.submit(function () {
                                    jQuery(this).find('input[placeholder]').each(function () {
                                        togglePlaceholder(jQuery(this), true);
                                    });
                                });
                            }
                        }
                    {% endif%}
                    
                    // Fluid video containers
                    var vidContainers;
                    var frameParentWidth;
                    var frameWidth;
                    var frameHeight;
                    var ratioFrame;
                    vidContainers = jQuery('.ob-section-video iframe, .ob-section-video object');
                    if (vidContainers.length > 0) {
                        vidContainers.each(function () {
                            frameParentWidth = jQuery(this).parent().innerWidth();
                            frameWidth = vidContainers.innerWidth();
                            if (frameWidth && frameParentWidth < frameWidth) {
                                frameHeight = vidContainers.innerHeight();
                                if (frameHeight) {
                                    ratioFrame = frameHeight / frameWidth;
                                    vidContainers.attr('width', frameParentWidth + 'px');
                                    vidContainers.attr('height', frameParentWidth * ratioFrame + 'px');
                                }
                            }
                        });
                    }
                    
                    
                    {% if Custom('custom_hide_widget_archives') == false %}
                    	// Archives
                        {% list Blog.Archives %}
                            switch_arch('{{Archive.year}}');
                        {% endlist %}
                    {% endif%}
                })();
            });
        </script>
        <!-- Placeholder script -->
		<script type="text/javascript">
			$('input[placeholder]').each(function(){  
		        var input = $(this);        		        
		        $(input).val(input.attr('placeholder'));
		        $(input).focus(function(){
		             if (input.val() == input.attr('placeholder')) {
		                 input.val('');
		             }
		        });
		        $(input).blur(function(){
		            if (input.val() == '' || input.val() == input.attr('placeholder')) {
		                input.val(input.attr('placeholder'));
		            }
		        });
		    });		
		</script>
    </body>
</html>